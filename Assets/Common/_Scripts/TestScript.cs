using UnityEngine;

public class TestScript : MonoBehaviour
{
    private int _nameVariable = 5;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int i = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
